# Phase 05 — 统计（DB + 监听）

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** 建 `MessageStat` 表、`DB Schema 初始化`、`stats_service` 与 `/api/stats`、`SendResponse` 监听器写计数。

**Architecture:** 单机部署，表无 `instance_id`。`services/stats_service.py` 查询；`models/stats.py` SQLAlchemy 模型；`__init__.py` startup `Base.metadata.create_all` + 注册 `SendResponse` 监听。

**Tech Stack:** entari_plugin_database（`Base`、`mapped_column`、`Mapped`、`get_session`、`SqlalchemyService`）、SQLAlchemy、arclet.entari（`SendResponse`、`get_plugins`）。

---

## 文件结构

- Create: `src/entari_plugin_webui/models/stats.py`
- Create: `src/entari_plugin_webui/services/stats_service.py`
- Create: `src/entari_plugin_webui/api/stats.py`
- Modify: `__init__.py`（startup 建表 + SendResponse 监听 + START_TIME）
- Modify: `api/router.py` 挂 router
- Modify: `tests/conftest.py`（database 配置）
- Create: `tests/services/test_stats_service.py`、`tests/api/test_stats.py`

---

## Task 5.1：MessageStat 模型

**Files:** Create `models/stats.py`

- [ ] **Step 1:**

```python
from __future__ import annotations

from entari_plugin_database import Base, Mapped, mapped_column
from sqlalchemy import Integer, String


class MessageStat(Base):
    __tablename__ = "webui_message_stat"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    platform: Mapped[str] = mapped_column(String(30), nullable=False)
    date: Mapped[str] = mapped_column(String(10), nullable=False, comment="YYYY-MM-DD")
    count: Mapped[int] = mapped_column(Integer, default=0)
```

- [ ] **Step 2:** 提交 `git commit -m "feat(model): MessageStat table"`。

---

## Task 5.2：stats_service

**Files:** Create `services/stats_service.py`

- [ ] **Step 1: 测试 `tests/services/test_stats_service.py`**（用 in-memory sqlite ）

由于 `stats_service` 使用 `get_session()` 异步上下文，测试中需要去 conftest 修改 database 插件的配置

```python
def pytest_configure(config: pytest.Config):
    config.stash[ENTARI_YML_TEXT] = """
basic:
  log:
    level: debug
plugins:
  database:
    name: :memory:
"""
    config.stash[ENTARI_WEBUI_CONFIG] = {}
```

然后写 `tests/services/test_stats_service.py`:


```python
from __future__ import annotations

from datetime import datetime, timedelta

import pytest
from sqlalchemy import select

from entari_plugin_webui.models.stats import MessageStat
from entari_plugin_webui.services import stats_service as ss


@pytest.mark.asyncio
async def test_today_messages():
    await ss.increment("qq")
    await ss.increment("qq")
    today = datetime.now().date().isoformat()
    got = await ss.today_messages()
    assert got == 2
```

- [ ] **Step 2: 实现 `services/stats_service.py`**

```python
from __future__ import annotations

from datetime import datetime, timedelta

from sqlalchemy import func, select

from ..models.stats import MessageStat

_START = datetime.now()


def _today() -> str:
    return datetime.now().date().isoformat()


async def increment(platform: str) -> None:
    from entari_plugin_database import get_session  # noqa: PLC0415

    today = _today()
    async with get_session() as session:
        result = await session.execute(
            select(MessageStat).where(MessageStat.platform == platform, MessageStat.date == today)
        )
        row = result.scalar_one_or_none()
        if row:
            row.count += 1
        else:
            session.add(MessageStat(platform=platform, date=today, count=1))
        await session.commit()


async def _sum(conditions) -> int:
    from entari_plugin_database import get_session  # noqa: PLC0415

    async with get_session() as session:
        result = await session.execute(select(func.coalesce(func.sum(MessageStat.count), 0)).where(*conditions))
        return int(result.scalar() or 0)


async def today_messages() -> int:
    return await _sum((MessageStat.date == _today(),))


async def week_messages() -> list[int]:
    today = datetime.now().date()
    monday = today - timedelta(days=today.weekday())
    days = [(monday + timedelta(days=i)).isoformat() for i in range(7)]
    from entari_plugin_database import get_session  # noqa: PLC0415

    out = [0] * 7
    async with get_session() as session:
        for i, d in enumerate(days):
            result = await session.execute(
                select(func.coalesce(func.sum(MessageStat.count), 0)).where(MessageStat.date == d)
            )
            out[i] = int(result.scalar() or 0)
    return out


async def total_messages() -> int:
    from entari_plugin_database import get_session  # noqa: PLC0415

    async with get_session() as session:
        result = await session.execute(select(func.coalesce(func.sum(MessageStat.count), 0)))
        return int(result.scalar() or 0)


async def get_stats() -> dict:
    from arclet.entari import get_plugins  # noqa: PLC0415

    plugins = list(get_plugins())
    enabled = sum(1 for p in plugins if p.is_available)
    try:
        today = await today_messages()
        week = await week_messages()
        total = await total_messages()
    except Exception:  # noqa: BLE001
        today, week, total = 0, [0] * 7, 0
    return {
        "today_messages": today,
        "week_messages": week,
        "total_messages": total,
        "plugins_enabled": enabled,
        "plugins_total": len(plugins),
        "runtime_minutes": int((datetime.now() - _START).total_seconds() // 60),
        "start_time": _START.isoformat(),
    }
```

- [ ] **Step 3:** Run tests → PASS（async pytest 需 `pytest-asyncio`）。

- [ ] **Step 4:** 加 `pytest-asyncio` 配置（pyproject `[tool.pytest.ini_options]` `asyncio_mode = "auto"`）。提交 `git commit -m "feat(stats): stats_service queries"`。

---

## Task 5.3：stats 路由

**Files:** Create `api/stats.py`

- [ ] **Step 1:**

```python
from __future__ import annotations

from fastapi import APIRouter, Depends

from ..api.deps import require_auth
from ..services.stats_service import get_stats

router = APIRouter(prefix="/api/stats", tags=["stats"], dependencies=[Depends(require_auth)])


@router.get("")
async def stats():
    return {"success": True, **(await get_stats())}
```

- [ ] **Step 2:** 挂 router。`pdm run format && pdm run lint && pdm run typecheck`。提交。

---

## Task 5.4：DB 建表 + SendResponse 监听

**Files:** Modify `__init__.py`

- [ ] **Step 1: 导入模型确保被 database 插件记录以建表**

```python
from .models.stats import MessageStat  # noqa: F401  ensure table registered

- [ ] **Step 2: SendResponse 监听**

```python
from arclet.entari.event.send import SendResponse
from .services.stats_service import increment


@plugin.listen(SendResponse)
async def _on_message_sent(event: SendResponse) -> None:
    platform = event.account.platform
    try:
        await increment(platform)
    except Exception as e:  # noqa: BLE001
        logger.debug(f"消息计数失败: {e}")
```

- [ ] **Step 3:** `pdm run test` 通过。提交 `git commit -m "feat(stats): DB init and SendResponse listener"`。

---

## Task 5.5：api 测试

**Files:** `tests/api/test_stats.py`

- [ ] **Step 1:**

```python
def test_stats(client, monkeypatch):
    from entari_plugin_webui.api import stats as S
    async def _get():
        return {"today_messages": 5, "week_messages": [1, 2, 3, 4, 5, 6, 7], "total_messages": 21,
                "plugins_enabled": 2, "plugins_total": 3, "runtime_minutes": 0, "start_time": "x"}
    monkeypatch.setattr(S, "get_stats", _get)
    r = client.get("/api/stats")
    assert r.json()["today_messages"] == 5
```

注意 `get_stats` 是 async，TestClient 会自动 await。`monkeypatch.setattr` 替换为 async 函数即可。

- [ ] **Step 2:** Run → PASS。提交。

---

## Phase 5 完成标准

- `/api/stats` 返回 today/week/total/plugins/runtime
- `MessageStat` 表启动建表，`SendResponse` 累加计数
- service 用内存 sqlite 单测通过；ruff/pyright/pytest 全绿