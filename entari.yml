# yaml-language-server: $schema=./entari.schema.json

basic:
  log:
    level: debug
    ignores:
      - "aiosqlite.core"
      - "asyncio.selector_events"
  schema: true
plugins:
  ::echo: {}
  server:
    port: 8765
    path: "satori"
  database:
    name: ":memory:"
  webui: {}
